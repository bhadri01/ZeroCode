/target
**/*.rs.bk
.DS_Store
*.swp
.env
.env.*
!.env.example
.idea/
.vscode/
*.log
/data/
runner-rootfs/
# `.sqlx/` is the offline query cache from `cargo sqlx prepare`. It IS
# committed so CI can build with `SQLX_OFFLINE=true` without a live DB.
